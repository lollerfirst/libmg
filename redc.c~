#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <stdint.h>

int inv_mod(int128_t* res, int128_t a, int128_t n)
{
  int128_t t = 1, r = a, old_t = 0, old_r = n;

  while (r != 0)
  {
    int128_t q = old_r / r;
    int128_t tmp = r;
    r = old_r - quotient * r;
    old_r = tmp;
    tmp = t;
    t = old_t - quotient * s;
    old_t = tmp; 
  }

  if (old_r > 1)
    return -1;

  if (old_t < 0)
    old_t += n

  *res = old_t; 
  return 0;
}





int main()
{
}
